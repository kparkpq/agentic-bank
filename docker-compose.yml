services:
  sandbox:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      PORT: "3000"
      DATABASE_PATH: /data/bank.sqlite
    volumes:
      - bankdata:/data

volumes:
  bankdata:
